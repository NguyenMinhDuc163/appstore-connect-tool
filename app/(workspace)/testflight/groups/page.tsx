import Link from"next/link";import{db}from"@/lib/db";import{Empty,PageHeader,Status}from"../../_components/ui";export default async function Groups(){const groups=await db.betaGroup.findMany({orderBy:[{app:{name:"asc"}},{name:"asc"}],include:{app:true,_count:{select:{testers:true,builds:true}}}});return <><PageHeader eyebrow="TestFlight" title="Beta groups" description="Internal and external tester groups, capacity and distribution policy."/>{groups.length?<div className="card overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="px-5 py-3">Group</th><th className="px-5 py-3">App</th><th className="px-5 py-3">Type</th><th className="px-5 py-3">Testers</th><th className="px-5 py-3">Builds</th><th className="px-5 py-3">Auto distribution</th></tr></thead><tbody className="divide-y">{groups.map(group=><tr key={group.id}><td className="px-5 py-4 font-semibold"><Link className="text-blue-700 hover:underline" href={`/testflight/groups/${group.id}`}>{group.name}</Link></td><td className="px-5 py-4">{group.app.name}</td><td className="px-5 py-4"><Status value={group.isInternal?"Internal":"External"}/></td><td className="px-5 py-4 font-mono">{group._count.testers}</td><td className="px-5 py-4 font-mono">{group._count.builds}</td><td className="px-5 py-4">{group.hasAccessToAllBuilds?"All eligible builds":"Selected builds"}</td></tr>)}</tbody></table></div>:<Empty title="No beta groups synced" text="Sync an app to load its TestFlight beta groups."/>}</>}
