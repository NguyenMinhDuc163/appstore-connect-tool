"use client";
import{useRouter}from"next/navigation";import{useState}from"react";export function FeedbackStatusActions({id,status}:{id:string;status:string}){const[busy,setBusy]=useState(false),router=useRouter();async function update(next:string){setBusy(true);await fetch(`/api/feedback/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status:next})});router.refresh();setBusy(false)}return <div className="flex flex-wrap gap-2"><button className="btn btn-primary" onClick={()=>update("RESOLVED")} disabled={busy||status==="RESOLVED"}>Mark resolved</button><button className="btn btn-secondary" onClick={()=>update("IN_PROGRESS")} disabled={busy||status==="IN_PROGRESS"}>In progress</button><button className="btn btn-secondary" onClick={()=>update("IGNORED")} disabled={busy||status==="IGNORED"}>Ignore</button></div>}
