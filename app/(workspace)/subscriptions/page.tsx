import{db}from"@/lib/db";import{Empty,PageHeader}from"../_components/ui";import{SubscriptionSearch}from"./subscription-search";export default async function Subscriptions(){const apps=await db.app.findMany({orderBy:{name:"asc"},select:{id:true,name:true}});return <><PageHeader eyebrow="App Store Server API" title="Subscriptions & IAP" description="Look up a customer using a transaction ID. Production and Sandbox are always explicit and never mixed."/>{apps.length?<SubscriptionSearch apps={apps}/>:<Empty title="No apps available" text="Sync apps before looking up subscription transactions."/>}</>}
