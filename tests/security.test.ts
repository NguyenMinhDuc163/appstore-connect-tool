import assert from"node:assert/strict";import test from"node:test";import{NextRequest}from"next/server";import{assertCron,assertSameOrigin}from"../lib/security";
test("same-origin mutations are accepted",()=>{assert.doesNotThrow(()=>assertSameOrigin(new NextRequest("https://ops.example/api/sync",{headers:{origin:"https://ops.example"}})))});
test("cross-origin mutations are rejected",()=>{assert.throws(()=>assertSameOrigin(new NextRequest("https://ops.example/api/sync",{headers:{origin:"https://evil.example"}})),/Invalid request origin/)});
test("cron requires an exact bearer secret",()=>{const previous=process.env.CRON_SECRET;process.env.CRON_SECRET="test-secret";try{assert.doesNotThrow(()=>assertCron(new NextRequest("https://ops.example/api/cron",{headers:{authorization:"Bearer test-secret"}})));assert.throws(()=>assertCron(new NextRequest("https://ops.example/api/cron",{headers:{authorization:"Bearer wrong"}})),/Unauthorized/)}finally{process.env.CRON_SECRET=previous}});
